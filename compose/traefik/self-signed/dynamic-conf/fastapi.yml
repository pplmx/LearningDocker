http:
    routers:
        fastapi:
            rule: "Host(`fastapi.x.io`)"
            service: "fastapi"
            tls: { }

    services:
        fastapi:
            loadBalancer:
                servers:
                    -   url: "http://fastapi:8080"
